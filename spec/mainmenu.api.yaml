# OpenApi header is included in this file to support code highlighting in VSCode
openapi: 3.0.3
info:
  title: Main Menu API
  version: "1.0.0"
paths:
  /main-menu:
    get:
      tags:
        - MainMenu
      summary: "Get Main Menu"
      description: "Get Main Menu"
      responses:
        "200":
          $ref: "#/components/responses/mainMenuGroup"
        "500":
          $ref: "generic_error_handling.xapi.yaml#/components/responses/genericErrorsResponse"
components:
  responses:
    mainMenuGroup:
      description: "Main Menu"
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "#/components/schemas/MainMenuGroup"
  schemas:
    MainMenuGroup:
      properties:
        name:
          type: string
        identifier:
          type: string
        description:
          type: string
        defaultOpen:
          type: boolean
        iconbase64:
          type: string
        items:
          type: array
          items:
            $ref: "#/components/schemas/MainMenuViewItem"
      required:
        - name
        - identifier
        - defaultOpen
        - iconbase64
        - items
    MainMenuViewItem:
      properties:
        viewIdentifier:
          type: string
      required:
        - viewIdentifier